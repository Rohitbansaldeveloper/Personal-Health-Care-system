# Ansible Inventory File for HealthCare Plus
# Update this file with your target Kubernetes controller/worker server details.

[all]
# By default, runs tasks on the local machine (for local clusters like Minikube or Docker Desktop)
localhost ansible_connection=local

# Example for remote Kubernetes controllers:
# k8s-master-node ansible_host=192.168.1.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
